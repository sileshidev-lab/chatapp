pipeline {
    agent any

    stages {
        stage('Placeholder') {
            steps {
                echo 'Jenkins pipeline will be implemented after service build/test steps are defined.'
            }
        }
    }
}

